# animated-cursors-forever
Required JS files for animated cursors on modern browsers.

This repository now includes a sample Chrome extension using **manifest_version 3**.
Load the folder in `chrome://extensions` with developer mode enabled to try it out.

The popup now loads its logic from popup.js to comply with Manifest V3's Content Security Policy.
